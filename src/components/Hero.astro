
---

---
<div class="relative z-20 h-[40rem] w-full overflow-hidden">
  <!-- VIDEO DE FONDO -->
  <video
    id="heroVideo"
    autoplay
    muted
    loop
    playsinline
    class="absolute top-0 left-0 w-full h-full object-cover z-[-1]"
  >
    <source src="/videos/bg-video-hero.webm" type="video/webm" />
  </video>

  <!-- HEADER -->
  <header
    class="flex justify-between items-center px-4 sm:px-8 py-4 text-white relative z-10"
  >
    <img src="/favicon.svg" alt="Logo" class="w-[50px] sm:w-[70px] md:w-[90px] hover:scale-110 transition-all duration-300 hover:cursor-pointer" loading="eager" />
    <!-- Lista con banderas disponibles -->
     <div class="flex gap-2 flex-col items-center">

       <ul class="flex gap-2 items-center hover:scale-110 transition-all duration-300">
         <li class="w-[30px] h-[30px]">
           <img src="/ico/arg.png" alt="Bandera de Argentina"/>
         </li>
         <li class="w-[30px] h-[30px]">
           <img src="/ico/cl.png" alt="Bandera de Chile" />
         </li>
         <li class="w-[30px] h-[30px]">
           <img src="/ico/br.png" alt="Bandera de Brasil" />
         </li>
         <li class="w-[30px] h-[30px]">
           <img src="/ico/uy.png" alt="Bandera de Uruguay" />
         </li>   
       </ul>
     </div>
    <!-- Menú móvil -->
    <button class="md:hidden text-white">
      <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
      </svg>
    </button>
  </header>

  <!-- CONTENIDO -->
  <div
    class="flex flex-col items-center justify-center text-center px-4 sm:px-6 h-full max-w-[90%] mx-auto text-white relative z-10 gap-4 sm:gap-6"
  >
    <h1
      class="text-2xl sm:text-4xl md:text-6xl font-extrabold drop-shadow-lg"
    >
      El caribe brasileño te espera, <br class="hidden sm:block"> en pesos y al mejor cambio
    </h1>

    <p class="text-sm sm:text-base md:text-lg text-white/85 leading-relaxed max-w-xl">
      En Alkimia Travel organizamos viajes exclusivos a Búzios, Brasil. Vos elegís la posada, nosotros nos encargamos del resto.
    </p>

    <a
      href="https://wa.me/5521992413485?text=Hola%20Juan%21%20Quiero%20cotizar%20unas%20vacaciones%21"
      target="_blank"
      class="mt-2 sm:mt-4 md:mt-6 inline-flex items-center gap-2 bg-[#00502F] hover:bg-[#1a3128] text-white font-semibold px-4 sm:px-6 py-2 sm:py-3 rounded-full shadow-lg transition text-sm sm:text-base animate-fade-in"
    >
      ¡Quiero mi viaje a Búzios!
    </a>

    <p class=" text-xs sm:text-sm text-white/90">
      +100 familias ya confiaron · +20 viajan todos los meses
    </p>
  </div>
</div>

<script>
  // Optimización de carga del video
  document.addEventListener('DOMContentLoaded', () => {
    const video = document.getElementById('heroVideo');
    if (video) {
      // Precargar el video
      (video as HTMLVideoElement).preload = 'auto';
      
      // Manejar errores de carga
      video.addEventListener('error', () => {
        console.error('Error al cargar el video');
      });

      // Optimizar reproducción
      video.addEventListener('canplay', () => {
        (video as HTMLVideoElement).play().catch((error: Error) => {
          console.log('Error al reproducir el video:', error);
        });
      });
    }
  });
</script>

<style>
@keyframes fade-in {
  from { opacity: 0; transform: translateY(30px); }
  to { opacity: 1; transform: translateY(0); }
}
.animate-fade-in {
  animation: fade-in 1s cubic-bezier(0.4,0,0.2,1) both;
}
</style>